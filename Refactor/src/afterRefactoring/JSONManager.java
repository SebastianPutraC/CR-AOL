// Nanti

/*package afterRefactoring;

import java.io.FileReader;

import org.json.simple.JSONArray;
import org.json.simple.JSONObject;
import org.json.simple.parser.JSONParser;


public class JSONManager 
{	
    JSONArray CustomerMovies = new JSONArray();
    JSONArray StoredMovies = new JSONArray();
    JSONArray RegisteredCustomer = new JSONArray();
    
    private void ParseData()
    {
    	for (int i = 0; i < CustomerMovies.size(); i++)
    	{
    		// Write Data based on Rental Object
    	}
    	for (int i = 0; i < StoredMovies.size(); i++)
    	{
    		// Write Data based on Rental Object
    	}
    	for (int i = 0; i < RegisteredCustomer.size(); i++)
    	{
    		// Write Data based on Customer Object
    	}
    }
    private void ReadData()
    {
    	JSONParser jsonParser = new JSONParser();
    	
    	JSONObject object = (JSONObject) jsonParser.parse(new FileReader("E:\Jason.json"));
    	
    	CustomerMovies.removeAll();
    	StoredMovies.removeAll();
    	RegisteredCustomer.removeAll();
    	
    	for (int i = 0; i < 20); i++) //20 as default
    	{
    		CustomerMovies.add(object);
    		// Read Data based on Rental Object
    	}
    	for (int i = 0; i < 20; i++)
    	{
    		StoredMovies.add(object);
    		// Read Data based on Rental Object
    	}
    	for (int i = 0; i < 20; i++)
    	{
    		RegisteredCustomer.add(Object);
    		// Read Data based on Customer Object
    	}
    }
}*/
